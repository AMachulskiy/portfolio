import { INavMenu } from '@src/interfaces/INavMenu'

const navMenu: INavMenu[] = [
  { to: '/', title: '_home' },
  { to: 'aboutme', title: '_about_me' },
  { to: 'projects', title: '_projects' },
]

export default navMenu
